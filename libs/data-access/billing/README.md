# billing

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build billing` to build the library.

## Running unit tests

Run `nx test billing` to execute the unit tests via [Vitest](https://vitest.dev/).
